package Characters;
import acm.graphics.*;

import java.awt.*;

public class red extends PlayableCharacter{

	public red(Color pantsColor, Color shirtColor, Color shoeColor, GCanvas canvas, double startingX, double startingY,
			String newPlayerName, GImage map, double initialHorizontalScaleFactor, double initialVerticalScaleFactor) {
		super(pantsColor, shirtColor, shoeColor, canvas, startingX, startingY, newPlayerName, map, initialHorizontalScaleFactor,
			initialVerticalScaleFactor);
		
	}

}
